import assert from 'node:assert';
import { describe, it } from 'node:test';
import { Queue } from '../Queue';

describe('Queue', () => {
    it('should queue', () => {
       const q = new Queue(); 
       assert(false);
    });
});
