import assert from 'node:assert';
import { describe, it } from 'node:test';
import { Stack } from '../Stack';

describe('Stack', () => {
    it('should stack', () => {
       const s = new Stack(); 
       assert(false);
    });
});
